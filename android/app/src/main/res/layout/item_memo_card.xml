<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/memo_card"
    android:orientation="horizontal"
    android:background="@drawable/bg_memo_card_surface"
    android:padding="12dp"
    android:layout_marginBottom="12dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- 🎨 ステータス丸（左側） -->
    <FrameLayout
        android:id="@+id/status_circle_container"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginEnd="12dp"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/status_circle"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center_vertical"/>
    </FrameLayout>

    <!-- 📝 本文＋日時＋ステータス名 -->
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1">

        <!-- 📝 本文 -->
        <TextView
            android:id="@+id/memo_content"
            android:text="買い物リスト：牛乳と卵"
            android:textColor="#000000"
            android:textSize="18sp"
            android:fontFamily="sans-serif-medium"
            android:maxLines="4"
            android:ellipsize="end"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp" />

        <!-- 🕒 日時 + ステータス名 -->
        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/memo_date"
                android:text="2025/10/15 00:47"
                android:textColor="#777777"
                android:textSize="14sp"
                android:fontFamily="sans-serif"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1" />

            <TextView
                android:id="@+id/status_nm"
                android:text="未完了"
                android:textColor="#4CAF50"
                android:textSize="14sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:layout_width="wrap_content"
                android:layout_height="match_parent" />
        </LinearLayout>
    </LinearLayout>
</LinearLayout>
