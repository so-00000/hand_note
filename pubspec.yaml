name: hand_note
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # ✅ GitHubのパッケージ直指定（subdirectoryを明示）
  home_widget:
    git:
      url: https://github.com/ABausG/home_widget.git
      ref: main
      path: packages/home_widget   # 👈 ここが超重要！

  cupertino_icons: ^1.0.8
  sqflite: ^2.3.3+1
  path_provider: ^2.1.3
  intl: ^0.19.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  another_flushbar: ^1.12.30

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

home_widget:
  android:
    package_name: com.example.hand_note
    enable_background_updates: true